const projects=[
 {name:'South Carolina Motors',slug:'south-carolina-motors',industry:'Automotive',image:'assets/scm-collision-center-cars-hero.jpg',alt:'Enhanced South Carolina Motors collision center image with vehicles',screen:'assets/south-carolina-motors.png',business:'South Carolina Motors helps drivers explore vehicles and collision repair support.',headline:'From vehicle interest to a real conversation.',challenge:'Shoppers compare quickly. Inventory, location, credibility, and the next step need to be clear.',approach:'Connect vehicle discovery and service information with local trust and straightforward contact paths.',focus:'Vehicle discovery & local trust',action:'Vehicle and estimate inquiries',services:['seo','google-ppc','google-business-profile','geo','social-media'],serviceCopy:'SEO, PPC, Google Business Profile, GEO, and social media.',category:'automotive',notes:['Make inventory and repair services easy to understand.','Show the business and its location clearly.','Keep calls and inquiries close to the decision.']},
 {name:'Rice’s Collision',slug:'rices-collision',industry:'Auto Body',image:'assets/rices-collision-hero.jpg',alt:'Rice’s Collision storefront and vehicles',screen:'assets/rices-collision.png',business:'Rice’s Collision helps West Hempstead drivers navigate auto body repairs and estimate requests.',headline:'Repair questions. A more reassuring next step.',challenge:'After an accident, drivers need to understand the shop, its services, and how to request an estimate.',approach:'Bring repair information, recognizable shop imagery, and estimate guidance into a clear customer journey.',focus:'Repair confidence & estimate clarity',action:'Repair estimate requests',services:['seo','google-ppc','google-business-profile','geo','social-media'],serviceCopy:'SEO, PPC, Google Business Profile, GEO, and social media.',category:'automotive',notes:['Lead with the shop and its repair capabilities.','Answer practical service and location questions.','Make the estimate path easy to find on mobile.']},
 {name:'H2Bros Plumbing',slug:'h2bros-plumbing',industry:'Home Services',image:'assets/h2bros-plumbing-hero.jpg',alt:'Bathroom sinks and plumbing fixtures featured by H2Bros',screen:'assets/h2bros-plumbing.png',business:'H2Bros Plumbing & Heating serves residential, commercial, and new-construction customers across New Jersey.',headline:'Clear services. A simpler way to ask for help.',challenge:'Urgent plumbing needs and planned installations require different information and clear contact options.',approach:'Organize service information around customer needs, with simple call and quote paths for each.',focus:'Service clarity & local discovery',action:'Plumbing and heating inquiries',services:['seo','google-ppc'],serviceCopy:'SEO and PPC focused on local visibility and plumbing inquiries.',category:'home',notes:['Distinguish urgent needs from planned work.','Make plumbing and heating services recognizable.','Keep phone and quote options within easy reach.']}
];
const services={'seo':'SEO','google-ppc':'PPC Advertising','google-business-profile':'Google Business Profile','geo':'AI Visibility & GEO','social-media':'Social Media Management'};
const directions=[['The Signature Edit','An oversized featured project, restrained typography, and a polished three-project collection.'],['Studio Casebook','An interactive dark split view: choose a client to reveal the business, brief, and services.'],['The Quiet Portfolio','A minimal cream directory with expandable case briefs and generous editorial spacing.'],['Work in Chapters','Cinematic project stories with full-width imagery and clear challenge-to-approach narratives.'],['Across Every Screen','A light digital showcase centered on existing client website presentations.'],['Find Your Parallel','A filterable project library that helps visitors find related industries and service combinations.'],['The Client Journal','A magazine-style cover story, two supporting features, and strategic field notes.'],['The Decision Trail','A visual narrative following the customer question, the strategy, and the intended next step.'],['The Project Briefs','A structured comparison of business context, services, and customer priorities.'],['Made to Connect','A bold blue opening and an interactive photo gallery, balanced with compact project summaries.']];
const nn=i=>String(i+1).padStart(2,'0');
const url=p=>`case-studies/${p.slug}.html`;
const image=(p,screen=false)=>`<img src="${screen?p.screen:p.image}" alt="${screen?p.name+' website presentation across devices':p.alt}" loading="lazy">`;
const button=(label='Discuss your project',href='contact.html')=>`<a class="button" href="${href}">${label}<span aria-hidden="true">↗</span></a>`;
const heading=(label,title,copy='')=>`<div class="section-heading"><p class="eyebrow">${label}</p><h2>${title}</h2>${copy?`<p class="lead">${copy}</p>`:''}</div>`;
const tags=p=>`<div class="tags">${p.services.map(s=>`<a href="services/${s}.html">${services[s]}</a>`).join('')}</div>`;
const nav=()=>`<header class="site-nav wrap"><a href="index.html"><img src="assets/vnwMedia-LogoBlk.png" alt="VNW Media home"></a><nav aria-label="Website navigation"><a href="our-story.html">Our Story</a><a href="work.html">Work</a><a href="services.html">Services</a><a href="industries.html">Industries</a><a href="case-studies.html" aria-current="page">Case Studies</a></nav>${button('Get Started')}</header>`;
const hero=(title,copy)=>`<section class="case-hero wrap">${heading('SELECTED CASE STUDIES',title,copy)}</section>`;
const read=p=>`<a class="text-link" href="${url(p)}">Read the case study ↗</a>`;
const projectCard=(p,i)=>`<article class="case-card"><a class="case-photo" href="${url(p)}">${image(p)}<span>${nn(i)}</span></a><p class="eyebrow">${p.industry}</p><h3><a href="${url(p)}">${p.name}</a></h3><p>${p.business}</p><p class="service-label">SERVICES PROVIDED</p><p>${p.serviceCopy}</p>${read(p)}</article>`;
function common(){return `<section class="case-principles section"><div class="wrap">${heading('THE THINKING BEHIND THE WORK','A business problem.<br>A connected approach.')}<div class="three-col">${[['01','Understand the decision.','Start with the business, its customers, and the questions that stand between interest and action.'],['02','Connect the experience.','Bring service messaging, search context, business proof, and contact paths into the same conversation.'],['03','Define what to measure.','Plan around meaningful actions such as calls and inquiries. Report performance only when supported by verified data.']].map(a=>`<article><span class="eyebrow">${a[0]}</span><h3>${a[1]}</h3><p>${a[2]}</p></article>`).join('')}</div></div></section>`;}
function ending(){return `<section class="section faq"><div class="wrap faq-grid">${heading('BEFORE YOUR NEXT PROJECT','A little more context.')}<div>${[
 ['Can you help a business like ours?','These projects show work across automotive and home services. Tell us about your audience, business model, and goals so we can discuss the right fit.'],['Do we need the same services as these clients?','No. We review your existing experience and priorities before recommending a scope. A focused project may be the right starting point.'],['Do these case studies include performance results?','These summaries focus on business context, services, and strategic direction. They do not claim unverified increases in traffic, leads, rankings, or revenue.'],['What happens in the first conversation?','We discuss your business, current website, customer journey, and the areas you want to improve. That context helps us identify practical next steps.']].map(a=>`<details><summary>${a[0]}</summary><p>${a[1]}</p></details>`).join('')}</div></div></section><section class="closing section"><div class="wrap"><p class="eyebrow">YOUR BUSINESS COULD BE NEXT</p><h2>Bring us the challenge.<br>Let’s find the direction.</h2><p>A website, a campaign, or a clearer path to your next customer.</p>${button('Talk through your project')}</div></section><footer class="site-footer wrap"><a href="index.html">VNW MEDIA</a><p>Websites. Visibility. Connected marketing.</p><a href="contact.html">Contact us</a></footer>`;}
function signature(){const p=projects[0];return hero('Work with context.<br><em>Stories with purpose.</em>','The businesses, services, and decisions behind selected VNW Media projects.')+`<section class="signature-feature wrap"><a href="${url(p)}">${image(p)}</a><div class="signature-caption"><div><p class="eyebrow">FEATURED / ${p.industry}</p><h2>${p.name}</h2></div><div><p>${p.headline}</p>${read(p)}</div></div></section><section class="section wrap">${heading('THE COLLECTION','Three businesses. Distinct priorities.')}<div class="three-col">${projects.map(projectCard).join('')}</div></section>`+common();}
function casebookPanel(p){return `<div class="casebook-photo">${image(p)}<p>${p.industry} / ${p.name}</p></div><div class="casebook-copy"><p class="eyebrow">THE BUSINESS</p><h2>${p.name}</h2><p>${p.business}</p><div class="brief-tabs" role="group" aria-label="Case study detail"><button data-detail="challenge" aria-pressed="true">The challenge</button><button data-detail="approach" aria-pressed="false">Our approach</button></div><p id="brief-text">${p.challenge}</p><p class="service-label">SERVICES PROVIDED</p>${tags(p)}${button('Read the full case',url(p))}</div>`;}
function casebook(){return hero('Every project starts<br>with <em>a real business.</em>','Select a client. Explore the challenge, the approach, and the services behind the work.')+`<section class="casebook wrap"><div class="client-tabs" role="group" aria-label="Choose a client">${projects.map((p,i)=>`<button data-client="${i}" aria-pressed="${i===0}"><span>${nn(i)}</span>${p.name}</button>`).join('')}</div><div id="casebook-panel" class="casebook-panel" aria-live="polite">${casebookPanel(projects[0])}</div></section>`+common();}
function quiet(){return hero('The work.<br><em>Without the noise.</em>','Three client stories. Clear business context, considered service choices, and a useful next step.')+`<section class="quiet-index wrap">${projects.map((p,i)=>`<details ${i===0?'open':''}><summary><span class="eyebrow">${nn(i)}</span><div><p class="eyebrow">${p.industry}</p><h2>${p.name}</h2></div><span class="quiet-plus" aria-hidden="true"></span></summary><div class="quiet-content">${image(p)}<div><h3>${p.headline}</h3><p>${p.business}</p><p>${p.approach}</p>${tags(p)}${read(p)}</div></div></details>`).join('')}</section>`+common();}
function chapters(){return `<section class="chapter-intro"><div class="wrap">${heading('CASE STUDIES','The story behind<br>the next <em>customer.</em>','Three projects. Three different moments of decision.')}<a class="text-link" href="#chapters">Explore the stories ↗</a></div></section><section id="chapters">${projects.map((p,i)=>`<article class="chapter"><div class="chapter-cover">${image(p)}<div class="wrap"><p class="eyebrow">CHAPTER ${nn(i)} / ${p.industry}</p><h2>${p.name}</h2><p>${p.headline}</p>${button('Read the case study',url(p))}</div></div><div class="chapter-notes wrap"><div><span class="eyebrow">THE CONTEXT</span><p>${p.business}</p></div><div><span class="eyebrow">THE APPROACH</span><p>${p.approach}</p></div><div><span class="eyebrow">SERVICES PROVIDED</span><p>${p.serviceCopy}</p></div></div></article>`).join('')}</section>`+common();}
function screens(){return hero('Business thinking.<br><em>On every screen.</em>','Explore existing client website presentations alongside the marketing services and customer priorities behind them.')+`<section class="screen-projects">${projects.map((p,i)=>`<article class="screen-project"><div class="wrap"><header><div><p class="eyebrow">${nn(i)} / ${p.industry}</p><h2>${p.name}</h2></div>${read(p)}</header><div class="screen-display">${image(p,true)}</div><div class="screen-caption"><h3>${p.headline}</h3><div><p>${p.business}</p><p class="service-label">SERVICES PROVIDED</p>${tags(p)}</div></div></div></article>`).join('')}</section>`+common();}
function finder(){return hero('Find the work<br><em>closest to your world.</em>','Browse by industry or service. See the business context first, then explore the full case.')+`<section class="case-finder wrap section"><div class="case-filters"><label>Industry<select id="industry-filter"><option value="all">All industries</option><option value="automotive">Automotive & Auto Body</option><option value="home">Home Services</option></select></label><label>Service<select id="service-filter"><option value="all">All services</option>${Object.entries(services).map(([s,n])=>`<option value="${s}">${n}</option>`).join('')}</select></label><button id="reset-filters">Reset filters</button><span id="case-count" role="status">3 projects</span></div><div id="case-results" class="three-col">${projects.map(projectCard).join('')}</div></section>`+common();}
function journal(){const p=projects[1];return `<section class="case-journal-head wrap"><p class="eyebrow">VNW MEDIA / CLIENT STORIES</p><h2>The Case<br><em>Study Journal.</em></h2><div><span>STRATEGY · SEARCH · CUSTOMER EXPERIENCE</span><p>A closer look at the practical decisions behind our client work.</p></div></section><section class="case-journal-feature wrap"><div>${image(p)}<p class="image-caption">IN FOCUS / WEST HEMPSTEAD AUTO BODY</p></div><div><p class="eyebrow">THE COVER STORY</p><h2>${p.headline}</h2><p>${p.business}</p><p>${p.approach}</p>${read(p)}</div></section><section class="section wrap"><div class="journal-support">${[projects[0],projects[2]].map(projectCard).join('')}</div></section><section class="case-pullquote section"><div class="wrap"><p class="eyebrow">OUR PERSPECTIVE</p><h2>The next click starts<br>with a better answer.</h2><p>Service clarity, business proof, and relevant information help visitors understand what to do next.</p></div></section>`+common();}
function trail(){return hero('Follow the decision.<br><em>Understand the work.</em>','A customer question becomes a strategic priority. Explore how each project connects that question to a useful next step.')+`<section class="decision-projects wrap">${projects.map((p,i)=>`<article class="decision-project"><header><span class="eyebrow">PROJECT ${nn(i)} / ${p.industry}</span><h2>${p.name}</h2></header><div class="decision-body"><div class="decision-visual">${image(p)}${read(p)}</div><ol>${[['The customer arrives',p.challenge],['The strategy responds',p.approach],['The intended next step',p.action+'. '+p.serviceCopy]].map((a,j)=>`<li><span>${nn(j)}</span><div><h3>${a[0]}</h3><p>${a[1]}</p></div></li>`).join('')}</ol></div></article>`).join('')}</section>`+common();}
function briefs(){return hero('Three projects.<br><em>The details that matter.</em>','Compare the business context and services side by side. Open any brief to explore the full project story.')+`<section class="section wrap"><div class="comparison-grid">${projects.map((p,i)=>`<article><span class="eyebrow">CASE ${nn(i)} / ${p.industry}</span>${image(p)}<h2>${p.name}</h2><div><span class="service-label">THE BUSINESS</span><p>${p.business}</p></div><div><span class="service-label">CUSTOMER PRIORITY</span><h3>${p.focus}</h3></div><div><span class="service-label">SERVICES PROVIDED</span>${tags(p)}</div><div><span class="service-label">APPROACH</span><p>${p.approach}</p></div>${read(p)}</article>`).join('')}</div></section><section class="scope-note section"><div class="wrap">${heading('FROM EXAMPLE TO YOUR BUSINESS','Same service does not mean the same scope.','Your customers, location, offer, and existing website all influence the work. We define priorities with you before recommending the service mix.')}${button('Discuss the right starting point')}</div></section>`+common();}
function galleryPanel(p,i){return `<div class="gallery-image">${image(p)}<span class="eyebrow">${nn(i)} / ${p.industry}</span></div><div class="gallery-caption"><div><p class="eyebrow">SELECTED CLIENT</p><h2>${p.name}</h2></div><div><p>${p.headline}</p>${read(p)}</div></div>`;}
function connect(){return `<section class="connect-hero section"><div class="wrap">${heading('VNW MEDIA / CASE STUDIES','Made to connect.<br>Built around <em>people.</em>','Real businesses. Practical priorities. Websites and marketing that belong in the same conversation.')}<div class="connect-labels"><span>BE FOUND</span><span>BUILD TRUST</span><span>MAKE ACTION EASIER</span></div></div></section><section class="case-gallery wrap section"><div class="gallery-controls"><p class="eyebrow">THE PROJECT GALLERY</p><div role="group" aria-label="Choose a featured project">${projects.map((p,i)=>`<button data-gallery="${i}" aria-pressed="${i===0}" aria-label="Show ${p.name}">${nn(i)}</button>`).join('')}</div></div><div id="gallery-panel" aria-live="polite">${galleryPanel(projects[0],0)}</div></section><section class="compact-cases section"><div class="wrap">${heading('BEHIND THE IMAGERY','The business. The focus. The work.')}<div class="three-col">${projects.map(p=>`<article><p class="eyebrow">${p.industry}</p><h3>${p.name}</h3><p>${p.business}</p><p class="service-label">SERVICES PROVIDED</p><p>${p.serviceCopy}</p>${read(p)}</article>`).join('')}</div></div></section>`+common();}
const renderers=[signature,casebook,quiet,chapters,screens,finder,journal,trail,briefs,connect];
let active=0,client=0;
const options=document.querySelector('#options');options.innerHTML=directions.map((d,i)=>`<button data-option="${i}" aria-pressed="false"><span>${nn(i)}</span>${d[0]}</button>`).join('');
function render(){const n=Number(location.hash.replace('#option-',''))-1;active=Number.isInteger(n)&&n>=0&&n<10?n:0;client=0;document.querySelectorAll('[data-option]').forEach((b,i)=>b.setAttribute('aria-pressed',i===active));document.querySelector('#option-number').textContent=nn(active);document.querySelector('#option-name').textContent=directions[active][0];document.querySelector('#option-description').textContent=directions[active][1];const preview=document.querySelector('#preview');preview.className=`case-concept case-option-${active+1}`;preview.innerHTML=nav()+renderers[active]()+ending();}
options.addEventListener('click',e=>{const b=e.target.closest('[data-option]');if(b)location.hash=`option-${+b.dataset.option+1}`;});
document.querySelector('#previous').onclick=()=>location.hash=`option-${(active+9)%10+1}`;
document.querySelector('#next').onclick=()=>location.hash=`option-${(active+1)%10+1}`;
document.querySelector('#share').onclick=async()=>{try{await navigator.clipboard.writeText(location.href);document.querySelector('#status').textContent='Option link copied.';}catch{document.querySelector('#status').textContent='Copy this page’s address from your browser to share the option.';}};
document.querySelector('#preview').addEventListener('click',e=>{const c=e.target.closest('[data-client]');if(c){client=+c.dataset.client;document.querySelectorAll('[data-client]').forEach(b=>b.setAttribute('aria-pressed',b===c));document.querySelector('#casebook-panel').innerHTML=casebookPanel(projects[client]);}const d=e.target.closest('[data-detail]');if(d){document.querySelectorAll('[data-detail]').forEach(b=>b.setAttribute('aria-pressed',b===d));document.querySelector('#brief-text').textContent=projects[client][d.dataset.detail];}const g=e.target.closest('[data-gallery]');if(g){const i=+g.dataset.gallery;document.querySelectorAll('[data-gallery]').forEach(b=>b.setAttribute('aria-pressed',b===g));document.querySelector('#gallery-panel').innerHTML=galleryPanel(projects[i],i);}if(e.target.closest('#reset-filters')){document.querySelector('#industry-filter').value='all';document.querySelector('#service-filter').value='all';filter();}});
document.querySelector('#preview').addEventListener('change',e=>{if(['industry-filter','service-filter'].includes(e.target.id))filter();});
function filter(){const category=document.querySelector('#industry-filter').value,service=document.querySelector('#service-filter').value;const found=projects.filter(p=>(category==='all'||p.category===category)&&(service==='all'||p.services.includes(service)));document.querySelector('#case-count').textContent=`${found.length} ${found.length===1?'project':'projects'}`;document.querySelector('#case-results').innerHTML=found.length?found.map(projectCard).join(''):`<div class="case-empty"><h3>No matching case study yet.</h3><p>Try another combination, or tell us about your project.</p>${button()}</div>`;}
window.addEventListener('hashchange',()=>{if(/^#option-\d+$/.test(location.hash))render();});render();
