/* Editorial stock photography reused under the sources/license recorded in
 assets/resources/photo-sources.json; not documentary photos of VNW staff. */
(()=>{
const names=['Seamless Cinema','Light & Shadow','The Wide Perspective','Immersive Chapters','Open Editorial'];
const stages=[
 {label:'Get to know you',title:'Your business.<br>Our starting point.',photo:0,text:'We listen to your goals, your customers, and where growth feels stuck. Then we review your website, search presence, competitors, and current campaigns to understand the opportunity.',sub:'A shared direction',points:['Business and audience priorities','Messaging and channel opportunities','Agreed scope and measurement starting point'],cta:'Talk through your goals',url:'contact.html'},
 {label:'Bring it to life',title:'Turn a clear direction<br>into work that matters.',photo:6,text:'The agreed plan becomes work you can see and review. Website experiences, search foundations, local profiles, and campaign creative connect around the same business priority.',sub:'Work you can review',points:['Design and implementation within scope','Connected website and marketing assets','Focused feedback and approval checkpoints'],cta:'Explore our services',url:'services.html'},
 {label:'Put it to work',title:'Go live.<br>With the details connected.',photo:1,text:'We prepare the selected channels and customer paths for launch. Mobile layouts, links, and agreed forms, integrations, and tracking are checked before the work reaches your audience.',sub:'A coordinated launch',points:['Selected campaign and channel setup','Customer-path and mobile checks','Final approval, launch, and handover'],cta:'Explore campaign services',url:'services/google-ppc.html'},
 {label:'Keep getting better',title:'What we learn<br>shapes what comes next.',photo:5,text:'When ongoing work is included, we review performance signals alongside your feedback on lead quality and customer needs. Together, we prioritize the next useful change.',sub:'Practical next moves',points:['Performance review against the starting point','Testing and prioritized refinements','Ongoing support within the agreed plan'],cta:'Plan your next step',url:'contact.html'}
];
const tag=x=>`<p class="px-tag">${x}</p>`;
const image=(s,cls='',eager=false)=>`<img class="${cls}" src="assets/resources/photo-${s.photo}.jpg" alt="${{0:'People discussing a project together over laptops',6:'A developer working at desktop monitors',1:'Colleagues collaborating in a bright office',5:'People reviewing a presentation together',10:'People working in an open creative studio'}[s.photo]}" loading="${eager?'eager':'lazy'}">`;
const cta=(text,url)=>`<a class="pill pill-blue" href="${url}">${text} <span aria-hidden="true">↗</span></a>`;
const content=(s,i)=>`${tag('0'+(i+1)+' / '+s.label)}<h2>${s.title}</h2><p class="px-lede">${s.text}</p><div class="px-outcomes"><h3>${s.sub}</h3><ul>${s.points.map(x=>`<li>${x}</li>`).join('')}</ul></div>${cta(s.cta,s.url)}`;
let observer;
function render(n){observer?.disconnect();let hero={photo:n===4?5:10};const rail=`<nav class="px-rail" aria-label="Our process stages">${stages.map((s,i)=>`<a href="#px-stage-${i}" data-jump="px-stage-${i}"><span>0${i+1}</span>${s.label}</a>`).join('')}</nav>`;
const sections=stages.map((s,i)=>`<article id="px-stage-${i}" class="px-stage px-stage-${i} ${n===2&&i%2===0?'px-light':''}" data-nav-theme="${n===2&&i%2===0?'light':'dark'}">${image(s,'px-stage-photo')}<div class="px-stage-shell"><div class="px-copy">${content(s,i)}</div></div></article>`).join('');
document.querySelector('#pp-page').innerHTML=`<div class="px-site px-option-${n}"><section class="px-hero" data-nav-theme="dark">${image(hero,'px-hero-photo',true)}<div class="px-shell">${tag('Our process')}<h1>Your next chapter.<br><em>Built around you.</em></h1><p>Clear thinking. Connected digital marketing.<br>Website experiences that support the work.</p>${cta('Let’s talk about your business','contact.html')}</div></section><section class="px-intro" data-nav-theme="light"><div class="px-shell">${tag('A partnership, not a handoff')}<h2>We get to know you.<br>Then we connect the pieces.</h2><p>Your goals shape the scope. Your feedback stays part of the conversation.<br>The next step is clear before the work moves forward.</p></div></section><section class="px-journey"><div class="px-rail-shell">${rail}</div><div class="px-chapters">${sections}</div></section><section class="px-finish" data-nav-theme="light"><div class="px-shell">${tag('A flexible framework')}<h2>The right services.<br>The right starting point.</h2><p>Search and AI discovery, paid and social campaigns, web design, and follow-up can share a direction without all being part of every project.</p><p class="px-scope">Deliverables, reviews, timing, and ongoing support are confirmed in your agreed project scope.</p><div class="px-finish-links">${cta('Find your starting point','contact.html')}<a href="services.html">Explore all services ↗</a><a href="work.html">See our work ↗</a></div></div></section></div>`;
document.querySelector('#pp-page').dataset.option=n;document.title=names[n-1]+' — Expanded Our Process | VNW Media';document.querySelectorAll('[data-option]').forEach(b=>b.setAttribute('aria-pressed',String(+b.dataset.option===n)));
document.querySelectorAll('[data-jump]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();document.getElementById(a.dataset.jump)?.scrollIntoView({block:'start',behavior:matchMedia('(prefers-reduced-motion:reduce)').matches?'instant':'smooth'});}));
observer=new IntersectionObserver(entries=>{for(let e of entries)if(e.isIntersecting){document.querySelectorAll('.px-rail a').forEach(a=>{if(a.dataset.jump===e.target.id)a.setAttribute('aria-current','step');else a.removeAttribute('aria-current');});}},{rootMargin:'-25% 0px -45% 0px',threshold:0});document.querySelectorAll('.px-stage').forEach(e=>observer.observe(e));
}
document.querySelector('#pp-options').innerHTML=names.map((s,i)=>`<button type="button" data-option="${i+1}" aria-pressed="false"><span>0${i+1}</span>${s}</button>`).join('');document.querySelector('#pp-options').addEventListener('click',e=>{let b=e.target.closest('[data-option]');if(!b)return;history.pushState(null,'','#option-'+b.dataset.option);render(+b.dataset.option);window.scrollTo({top:0,behavior:'instant'});});const load=()=>render(+location.hash.match(/^#option-([1-5])$/)?.[1]||1);window.addEventListener('popstate',load);window.addEventListener('hashchange',load);load();
})();
