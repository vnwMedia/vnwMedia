/* Additional concepts 11–20. Shared client facts remain in the original review script. */
const moreDirections=[
 ['Business, Front & Center','A large typographic opening, offset photo composition, and spacious client spotlights.'],
 ['The Project Desk','An asymmetric studio desk: website presentations, service notes, and business briefs in one composition.'],
 ['Open Gallery','A dark expanding photo gallery with an in-depth project brief below the selected client.'],
 ['Through the Buyer’s Lens','A question-led portfolio that lets prospects view every project through challenge, approach, or scope.'],
 ['Collected Case Files','Layered editorial case sheets with large numbers, client imagery, and concise decision notes.'],
 ['The Partnership Essay','A split editorial layout with a persistent introduction and rich project stories alongside it.'],
 ['Local Business, Big Presence','Bold blue and black bands pairing real business imagery with clear marketing priorities.'],
 ['The Shortlist','A clean project contact sheet and interactive two-project comparison for practical buyers.'],
 ['Inside the Experience','An annotated website gallery with selectable observations and transparent service scope.'],
 ['Start With Your Question','A guided introduction that connects a visitor’s priority to relevant case studies and a planning conversation.']
];
const caseQuestionData=[
 ['What if people cannot find us?','Start with how customers search.','Review service content, local business information, technical foundations, and paid-search relevance before adding more channels.','seo','Explore SEO',0],
 ['What if visitors do not contact us?','Look at the decision before the button.','Review whether customers can understand the offer, trust the business, and find a clear path to a call or inquiry.','landing-pages','Explore landing pages',1],
 ['What if our services are hard to understand?','Make the offer easier to navigate.','Organize the website around practical customer needs, distinguish different service paths, and bring useful answers forward.','web-design','Explore web design',2]
];
function newJump(){return `<nav class="new-jump wrap" aria-label="Case studies page sections"><a href="#new-work">The client work</a><a href="#new-fit">Is this a fit?</a><a href="#new-faq">Questions & answers</a><a href="contact.html">Start a conversation ↗</a></nav>`;}
function caseEvidence(p){return `<dl class="case-evidence"><div><dt>Business context</dt><dd>${p.business}</dd></div><div><dt>Services provided</dt><dd>${p.serviceCopy}</dd></div><div><dt>Intended customer action</dt><dd>${p.action}</dd></div></dl>`;}
function moreEnd(mode=0){return `<section class="new-fit section fit-${mode%3}" id="new-fit"><div class="wrap">${heading('IS THIS THE RIGHT FIT?','Choose the work your business actually needs.','A relevant case study is a starting point—not a promise that another business will get the same outcome.')}<div class="fit-paths"><article><span class="eyebrow">A FOCUSED START</span><h3>One problem.<br>A clear scope.</h3><p>Useful when a specific website issue, search priority, or campaign needs attention.</p><ul><li>Agree on the business goal and priority audience.</li><li>Define the deliverables and what is out of scope.</li><li>Choose the signals that will show useful progress.</li></ul><a class="text-link" href="contact.html">Discuss a focused project ↗</a></article><article><span class="eyebrow">A CONNECTED PLAN</span><h3>Several channels.<br>A shared direction.</h3><p>Useful when your website, visibility, messaging, and follow-up need to work together.</p><ul><li>Review the customer journey across channels.</li><li>Sequence the work around your team and priorities.</li><li>Agree on ownership, reporting, and ongoing support.</li></ul><a class="text-link" href="contact.html">Discuss a connected plan ↗</a></article></div></div></section><section class="new-invitation"><div class="wrap"><div><p class="eyebrow">RECOGNIZE THE CHALLENGE?</p><h2>Your project does not have to look like theirs.</h2><p>Bring your business context. We’ll discuss what should be different.</p></div>${button('Let’s discuss your priorities')}</div></section><section class="new-expect section"><div class="wrap">${heading('BEFORE YOU COMMIT','Know what you are agreeing to.')}<div class="expect-grid">${[
 ['The scope','Ask for clear deliverables, platforms, integrations, review rounds, and exclusions in the proposal.'],['The collaboration','Confirm who supplies content, approves the work, provides access, and handles day-to-day questions.'],['The investment & timing','Budget and schedule depend on the work, content readiness, integrations, and approvals. Confirm both before starting.'],['The evidence','Separate what was delivered from measured outcomes. Agree on tracking, useful reporting, and the limits of the available data.']].map((a,i)=>`<article><span class="eyebrow">${nn(i)}</span><h3>${a[0]}</h3><p>${a[1]}</p></article>`).join('')}</div></div></section><section class="section faq new-faq" id="new-faq"><div class="wrap faq-grid"><div>${heading('MAKE AN INFORMED DECISION','Good questions.<br>Clear answers.')}<p>Ask us about the work, the scope, and what a practical starting point could look like for your business.</p><a class="text-link" href="contact.html">Ask about your project ↗</a></div><div>${[
 ['How relevant are these projects to my business?','Look for a similar customer decision or marketing challenge, not just the same industry. These examples cover automotive and home services; we can discuss how your audience and business model differ.'],['Which services were actually provided?','The current portfolio lists SEO, PPC, Google Business Profile, GEO, and social media for South Carolina Motors and Rice’s Collision. H2Bros lists SEO and PPC. Website imagery and strategic observations provide context; they do not expand that service list.'],['Are the results measured or guaranteed?','These summaries describe business context, services, and strategic direction. No traffic, lead, ranking, or revenue increases are presented as verified results. Outcomes vary, and rankings, AI citations, and sales are not guaranteed.'],['Can we start with a single service?','Yes. A focused website, search, or campaign project may be a useful first step. We review the dependencies and agree on the scope before recommending additional work.'],['How much will it cost, and how long will it take?','There is no single price or timeline for these examples. Your deliverables, content, integrations, and approval process affect both. The proposal should set out the agreed investment, schedule, and assumptions.'],['What will you need from our team?','Usually a clear business goal, access to the relevant website and marketing accounts, available brand and content materials, and someone who can make timely decisions. Exact requirements depend on the scope.'],['How should we evaluate progress?','Agree on meaningful measures before launch. Depending on the project, these could include qualified inquiries, call actions, service discovery, or campaign lead quality. Revenue claims require verified business data.'],['What happens after launch or delivery?','Discuss maintenance, campaign management, reporting, and ongoing improvement separately. Confirm which support is included, who owns each task, and what requires additional scope.']].map(a=>`<details><summary>${a[0]}</summary><p>${a[1]}</p></details>`).join('')}</div></div></section><section class="closing section"><div class="wrap"><p class="eyebrow">A CLEARER STARTING POINT</p><h2>Let’s talk about<br>what your business needs.</h2><p>Share your website, your audience, and the part of the journey that needs attention.</p>${button('Start the conversation')}</div></section><footer class="site-footer wrap"><a href="index.html">VNW MEDIA</a><p>Websites. Visibility. Connected marketing.</p><a href="contact.html">Contact us</a></footer>`;}
function businessFront(){return `<section class="front-hero wrap"><div><p class="eyebrow">VNW MEDIA / SELECTED CLIENT WORK</p><h2>Business<br>comes <em>first.</em></h2><p>Understand the customer needs behind the marketing brief. Then explore the services that support them.</p></div><div class="front-photos">${projects.map((p,i)=>`<a href="${url(p)}">${image(p)}<span>${nn(i)} / ${p.name}</span></a>`).join('')}</div></section>${newJump()}<section class="front-stories section wrap" id="new-work">${projects.map((p,i)=>`<article><div class="front-number">${nn(i)}</div><div><span class="eyebrow">${p.industry}</span><h2>${p.headline}</h2><p>${p.business}</p>${caseEvidence(p)}${read(p)}</div><a href="${url(p)}">${image(p,true)}</a></article>`).join('')}</section>`;}
function projectDesk(){return `<section class="desk-hero section"><div class="wrap desk-grid"><div class="desk-title"><p class="eyebrow">THE VNW MEDIA PROJECT DESK</p><h2>More than<br>the finished<br><em>picture.</em></h2><p>The business brief, the services, and the decisions behind the work.</p></div><div class="desk-screen">${image(projects[1],true)}<span>RICE’S COLLISION / WEBSITE PRESENTATION</span></div><div class="desk-note"><p class="eyebrow">THE QUESTION THAT MATTERS</p><h3>What should the customer understand next?</h3></div><div class="desk-strip">${projects.map(p=>`<a href="${url(p)}">${p.name} ↗</a>`).join('')}</div></div></section>${newJump()}<section class="section wrap" id="new-work">${heading('OPEN THE BRIEF','A different business on every page.')}<div class="desk-cases">${projects.map((p,i)=>`<article><header><span>${nn(i)}</span><h2>${p.name}</h2></header><div class="desk-case-body">${image(p)}<div><p class="eyebrow">THE BUSINESS</p><p>${p.business}</p><h3>The challenge</h3><p>${p.challenge}</p><h3>The direction</h3><p>${p.approach}</p></div></div><footer><div><p class="eyebrow">SERVICES PROVIDED</p>${tags(p)}</div>${read(p)}</footer></article>`).join('')}</div></section>`;}
function openGalleryBrief(i){const p=projects[i];return `<div><p class="eyebrow">THE BUSINESS / ${p.industry}</p><h2>${p.headline}</h2><p>${p.business}</p>${read(p)}</div><div><h3>The challenge</h3><p>${p.challenge}</p><h3>Our approach</h3><p>${p.approach}</p><p class="service-label">SERVICES PROVIDED</p>${tags(p)}</div>`;}
function openGallery(){return `<section class="open-gallery-hero wrap"><p class="eyebrow">CASE STUDIES / EXPLORE THE WORK</p><h2>Real businesses.<br><em>Individual stories.</em></h2><p>Open a project to see beyond the image.</p></section>${newJump()}<section class="section wrap" id="new-work"><div class="expanding-gallery" role="group" aria-label="Select a project">${projects.map((p,i)=>`<button data-expand="${i}" aria-pressed="${i===0}">${image(p)}<span class="expand-copy"><small>${nn(i)} / ${p.industry}</small><strong>${p.name}</strong><span>Explore the brief ↗</span></span></button>`).join('')}</div><div id="expanded-brief" class="expanded-brief" aria-live="polite">${openGalleryBrief(0)}</div></section>`;}
function lensCards(key){return projects.map((p,i)=>`<article>${image(p)}<span class="eyebrow">${nn(i)} / ${p.industry}</span><h3>${p.name}</h3><p class="lens-answer">${key==='scope'?p.serviceCopy:p[key]}</p>${read(p)}</article>`).join('');}
function buyerLens(){return `<section class="lens-hero wrap"><p class="eyebrow">CASE STUDIES / LOOK CLOSER</p><h2>Don’t just ask<br><em>“Does it look good?”</em></h2><p>Ask what the business needed, how the work responds, and which services were involved.</p></section>${newJump()}<section class="lens-work section" id="new-work"><div class="wrap"><div class="lens-controls" role="group" aria-label="Choose a project perspective">${[['challenge','What needed attention?'],['approach','What was the direction?'],['scope','What services were provided?']].map((a,i)=>`<button data-lens="${a[0]}" aria-pressed="${i===0}"><span>${nn(i)}</span>${a[1]}</button>`).join('')}</div><div class="lens-cards" id="lens-cards" aria-live="polite">${lensCards('challenge')}</div></div></section><section class="lens-statement section"><div class="wrap"><p class="eyebrow">A BETTER WAY TO EVALUATE THE WORK</p><h2>The strongest connection<br>is to <em>your customer’s decision.</em></h2><p>Use the full case studies to assess relevance. Similar challenges can inform the conversation without implying identical results.</p></div></section>`;}
function collectedFiles(){return `<section class="files-hero wrap"><p class="eyebrow">VNW MEDIA / CASE FILES</p><h2>Selected.<br>Considered.<br><em>Connected.</em></h2><div><p>Three client files, opened around the questions a prospective partner should ask.</p><a href="#new-work" class="text-link">Open the collection ↗</a></div></section>${newJump()}<section class="case-files wrap" id="new-work">${projects.map((p,i)=>`<article class="case-file"><header><span>CASE FILE ${nn(i)}</span><span>${p.industry}</span></header><div class="file-main"><div><h2>${p.name}</h2><p>${p.business}</p><h3>${p.focus}</h3>${tags(p)}${read(p)}</div>${image(p)}</div><div class="file-notes"><div><span class="eyebrow">THE CHALLENGE</span><p>${p.challenge}</p></div><div><span class="eyebrow">THE APPROACH</span><p>${p.approach}</p></div></div></article>`).join('')}</section>`;}
function partnershipEssay(){return `${newJump()}<section class="essay-layout wrap section" id="new-work"><aside><p class="eyebrow">SELECTED CASE STUDIES</p><h2>Good work.<br>Clear thinking.<br><em>Both matter.</em></h2><p>A portfolio should help you understand the business, the choices, and the scope—not just the surface.</p>${button('Talk about your project')}<nav aria-label="Jump to client">${projects.map((p,i)=>`<a href="#essay-${i}">${nn(i)} / ${p.name}</a>`).join('')}</nav></aside><div>${projects.map((p,i)=>`<article id="essay-${i}" class="essay-case"><p class="eyebrow">${nn(i)} / ${p.industry}</p>${image(p)}<h2>${p.name}</h2><p class="essay-lead">${p.business}</p><h3>${p.headline}</h3><p>${p.challenge}</p><p>${p.approach}</p><p class="service-label">SERVICES PROVIDED</p>${tags(p)}${read(p)}</article>`).join('')}</div></section>`;}
function localPresence(){return `<section class="presence-hero"><div class="wrap"><p class="eyebrow">LOCAL BUSINESSES / CONNECTED MARKETING</p><h2>Be known.<br>Be understood.<br><em>Be considered.</em></h2><p>Explore how the customer’s next decision shapes the services behind our client work.</p></div><div class="presence-rail">${projects.map(p=>image(p)).join('')}</div></section>${newJump()}<section id="new-work">${projects.map((p,i)=>`<article class="presence-case presence-${i}"><div class="wrap"><div class="presence-heading"><span>${nn(i)}</span><h2>${p.name}</h2></div><div class="presence-body">${image(p)}<div><p class="eyebrow">${p.industry}</p><h3>${p.headline}</h3><p>${p.business}</p><p>${p.approach}</p><p class="service-label">SERVICES PROVIDED</p>${tags(p)}${read(p)}</div></div></div></article>`).join('')}</section>`;}
function shortlistCard(p){return `<article><p class="eyebrow">${p.industry}</p><h2>${p.name}</h2>${image(p)}${caseEvidence(p)}<h3>Strategic direction</h3><p>${p.approach}</p>${read(p)}</article>`;}
function shortlist(){return `<section class="shortlist-hero wrap"><div><p class="eyebrow">CASE STUDIES / BUILD YOUR PERSPECTIVE</p><h2>Put the right<br>work on <em>your shortlist.</em></h2><p>Compare the business needs and service mix. Decide what is relevant before discussing your own scope.</p></div><div class="shortlist-thumbs">${projects.map((p,i)=>`<a href="${url(p)}">${image(p)}<span>${nn(i)} / ${p.name}</span></a>`).join('')}</div></section>${newJump()}<section class="section wrap" id="new-work">${heading('COMPARE THE CONTEXT','Two projects. A clearer perspective.')}<div class="compare-selects">${[0,1].map((n)=>`<label>Project ${n+1}<select data-compare="${n}">${projects.map((p,i)=>`<option value="${i}" ${i===n?'selected':''}>${p.name}</option>`).join('')}</select></label>`).join('')}</div><div class="shortlist-compare" id="shortlist-compare" aria-live="polite">${shortlistCard(projects[0])+shortlistCard(projects[1])}</div><p class="comparison-disclaimer">Compare scope and context, not an unverified performance ranking. Explore each full case for more detail.</p></section>`;}
function insideExperience(){return `<section class="inside-hero wrap"><p class="eyebrow">CASE STUDIES / THE CUSTOMER EXPERIENCE</p><h2>Look at the screen.<br><em>Think about the person.</em></h2><p>Explore the practical customer needs behind each existing website presentation.</p></section>${newJump()}<section class="inside-projects" id="new-work">${projects.map((p,i)=>`<article class="section"><div class="wrap"><header><p class="eyebrow">${nn(i)} / ${p.industry}</p><h2>${p.name}</h2></header><div class="inside-grid"><div class="inside-screen">${image(p,true)}<div role="group" aria-label="Explore ${p.name} customer priorities">${p.notes.map((n,j)=>`<button data-inside="${i}" data-note="${j}" aria-pressed="${j===0}" aria-label="Customer priority ${j+1} for ${p.name}">${nn(j)}</button>`).join('')}</div></div><div class="inside-copy"><p>${p.business}</p><div class="inside-observation" id="inside-${i}" aria-live="polite"><span class="eyebrow">CUSTOMER PRIORITY 01</span><h3>${p.notes[0]}</h3></div><p class="service-label">SERVICES PROVIDED</p>${tags(p)}<p class="inside-caption">Website presentation shown for context. Customer priorities describe the strategic direction, not measured outcomes.</p>${read(p)}</div></div></div></article>`).join('')}</section>`;}
function questionAnswer(i){const q=caseQuestionData[i],p=projects[q[5]];return `<div><p class="eyebrow">A USEFUL STARTING POINT</p><h2>${q[1]}</h2><p>${q[2]}</p><a class="text-link" href="services/${q[3]}.html">${q[4]} ↗</a></div><article>${image(p)}<div><p class="eyebrow">RELATED CASE / ${p.industry}</p><h3>${p.name}</h3><p>${p.focus}</p>${read(p)}</div></article>`;}
function startQuestion(){return `<section class="question-hero wrap"><div><p class="eyebrow">CASE STUDIES / FIND YOUR STARTING POINT</p><h2>Your next move<br>starts with<br><em>a better question.</em></h2><p>Use the work to ask what your own customers need—not to copy someone else’s plan.</p></div><div class="question-photo">${image(projects[2])}<span>REAL BUSINESS CONTEXT.<br>PRACTICAL NEXT STEPS.</span></div></section>${newJump()}<section class="question-guide section"><div class="wrap"><p class="eyebrow">WHAT FEELS STUCK?</p><div class="question-buttons" role="group" aria-label="Choose a business question">${caseQuestionData.map((q,i)=>`<button data-question="${i}" aria-pressed="${i===0}">${q[0]}</button>`).join('')}</div><div id="question-answer" class="question-answer" aria-live="polite">${questionAnswer(0)}</div><p class="question-disclaimer">This is a conversation guide, not an audit or a fixed service recommendation.</p></div></section><section class="section wrap" id="new-work">${heading('EXPLORE ALL THREE CLIENTS','See the context behind the work.')}<div class="three-col">${projects.map(projectCard).join('')}</div></section>`;}
function bindMoreInteractions(){document.querySelector('#preview').addEventListener('click',e=>{let b=e.target.closest('[data-expand]');if(b){document.querySelectorAll('[data-expand]').forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelector('#expanded-brief').innerHTML=openGalleryBrief(+b.dataset.expand);}b=e.target.closest('[data-lens]');if(b){document.querySelectorAll('[data-lens]').forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelector('#lens-cards').innerHTML=lensCards(b.dataset.lens);}b=e.target.closest('[data-inside]');if(b){const i=+b.dataset.inside,j=+b.dataset.note;document.querySelectorAll(`[data-inside="${i}"]`).forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelector(`#inside-${i}`).innerHTML=`<span class="eyebrow">CUSTOMER PRIORITY ${nn(j)}</span><h3>${projects[i].notes[j]}</h3>`;}b=e.target.closest('[data-question]');if(b){document.querySelectorAll('[data-question]').forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelector('#question-answer').innerHTML=questionAnswer(+b.dataset.question);}});document.querySelector('#preview').addEventListener('change',e=>{if(e.target.matches('[data-compare]')){const selects=[...document.querySelectorAll('[data-compare]')];if(selects[0].value===selects[1].value){const other=selects.find(x=>x!==e.target);other.value=String((+e.target.value+1)%projects.length);}document.querySelector('#shortlist-compare').innerHTML=selects.map(s=>shortlistCard(projects[+s.value])).join('');}});}
